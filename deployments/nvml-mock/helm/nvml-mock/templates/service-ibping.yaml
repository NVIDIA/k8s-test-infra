# Copyright 2026 NVIDIA CORPORATION
# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nvml-mock.fullname" . }}-ibping
  labels:
    {{- include "nvml-mock.labels" . | nindent 4 }}
spec:
  # Headless: DNS returns all nvml-mock pod IPs for fabric peer discovery.
  clusterIP: None
  selector:
    {{- include "nvml-mock.selectorLabels" . | nindent 4 }}
  ports:
    - name: ibping
      port: {{ .Values.infiniband.ping.port }}
      targetPort: ibping
      protocol: TCP
