{{- /* Copyright 2026 NVIDIA CORPORATION */}}
{{- /* SPDX-License-Identifier: Apache-2.0 */}}
{{- if and .Values.infiniband.ping.networkPolicy.enabled (eq (include "nvml-mock.infinibandEnabled" .) "true") }}
# The fabric relay listens on 0.0.0.0:{{ .Values.infiniband.ping.port }} without
# authentication, in a pod that also runs a privileged container. Admitting only
# peer nvml-mock pods keeps that surface reachable by its real clients alone.
# Egress stays open so DNS peer discovery and registration still work.
#
# kindnetd enforces NetworkPolicy on current Kind releases, so this is
# load-bearing in local e2e, not only on Calico/Cilium.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "nvml-mock.fullname" . }}-ibping
  labels:
    {{- include "nvml-mock.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "nvml-mock.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: daemon
  policyTypes:
    - Ingress
  ingress:
    - from:
        # Name + component, not instance: the fabric spans every daemon pod
        # cluster-wide and ibping-multinode deploys one release per node, so
        # instance scoping would drop cross-release REGISTER. The component
        # label keeps the NRI sidecar out.
        #
        # namespaceSelector {} is load-bearing, not redundant: a bare
        # podSelector matches only this policy's own namespace, silently
        # dropping REGISTER from a fleet split across namespaces.
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              app.kubernetes.io/name: {{ include "nvml-mock.name" . }}
              app.kubernetes.io/component: daemon
      ports:
        - port: ibping
          protocol: TCP
{{- end }}
