# Copyright 2026 NVIDIA CORPORATION
# SPDX-License-Identifier: Apache-2.0
suite: IB ping Service
templates:
  - templates/service-ibping.yaml
tests:
  - it: should not render when ping is disabled
    asserts:
      - hasDocuments:
          count: 0

  - it: should render a headless Service when ping is enabled
    set:
      infiniband:
        ping:
          enabled: true
          port: 18515
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Service
      - equal:
          path: metadata.name
          value: RELEASE-NAME-nvml-mock-ibping
      - equal:
          path: spec.clusterIP
          value: None
      - equal:
          path: spec.selector["app.kubernetes.io/name"]
          value: nvml-mock
      - equal:
          path: spec.selector["app.kubernetes.io/instance"]
          value: RELEASE-NAME
      - equal:
          path: spec.ports[0].name
          value: ibping
      - equal:
          path: spec.ports[0].port
          value: 18515
      - equal:
          path: spec.ports[0].targetPort
          value: ibping
      - equal:
          path: spec.ports[0].protocol
          value: TCP

  - it: should use a custom ping port
    set:
      infiniband:
        ping:
          enabled: true
          port: 19000
    asserts:
      - equal:
          path: spec.ports[0].port
          value: 19000
