# Copyright 2026 NVIDIA CORPORATION
# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nvidia-mock.fullname" . }}-config
  labels:
    {{- include "nvidia-mock.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "nvidia-mock.gpuConfig" . | nindent 4 }}
